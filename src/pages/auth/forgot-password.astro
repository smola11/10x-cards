---
import Layout from "../../layouts/Layout.astro";
import ForgotPasswordForm from "@/components/auth/ForgotPasswordForm";
---

<Layout title="Reset hasła | 10x Cards">
  <main class="min-h-screen bg-gradient-to-b from-background via-background to-muted/20 text-foreground">
    <section class="mx-auto flex min-h-screen max-w-6xl flex-col justify-center px-4 py-12 sm:px-6 lg:px-8">
      <div class="grid gap-12 lg:grid-cols-[minmax(0,0.55fr)_minmax(0,0.45fr)] lg:items-center">
        <header class="space-y-6 rounded-2xl border border-dashed border-border/60 bg-muted/10 p-8">
          <p class="text-sm font-semibold uppercase tracking-wide text-primary/80">Potrzebujesz pomocy?</p>
          <h1 class="text-3xl font-semibold tracking-tight text-foreground md:text-4xl">Przywróć dostęp do konta</h1>
          <p class="text-base text-muted-foreground md:text-lg">
            Podaj adres email powiązany z kontem. Wyślemy instrukcję resetu hasła. Dla bezpieczeństwa nie ujawniamy,
            czy konto istnieje.
          </p>
          <ul class="grid gap-3 text-sm text-muted-foreground">
            <li class="flex items-center gap-3">
              <span class="grid size-6 place-items-center rounded-full bg-primary/10 text-primary">✓</span>
              Link resetujący będzie ważny przez określony czas.
            </li>
            <li class="flex items-center gap-3">
              <span class="grid size-6 place-items-center rounded-full bg-primary/10 text-primary">✓</span>
              Po zmianie hasła wpiszesz się ponownie do aplikacji.
            </li>
          </ul>
        </header>

        <div class="flex justify-center lg:justify-end">
          <ForgotPasswordForm client:idle />
        </div>
      </div>
    </section>
  </main>
</Layout>

